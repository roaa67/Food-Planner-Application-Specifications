<?xml version="1.0" encoding="utf-8"?>
<!--
    Country Item Layout (course p334 - "Create a list item (custom row) layout")
    Shows flag emoji + country name
    Touch target: minimum 48dp x 48dp (course p192)
-->
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="90dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="@dimen/spacing_sm"
    android:clickable="true"
    android:focusable="true"
    app:cardCornerRadius="@dimen/card_radius"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="@dimen/spacing_sm"
        android:minHeight="@dimen/touch_target_min">

        <!-- Country Flag (emoji as text) or image -->
        <TextView
            android:id="@+id/tv_country_flag"
            android:layout_width="@dimen/country_flag_size"
            android:layout_height="@dimen/country_flag_size"
            android:contentDescription="@string/cd_country_flag"
            android:gravity="center"
            android:textSize="28sp" />

        <!-- Country Name -->
        <TextView
            android:id="@+id/tv_country_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_xs"
            android:gravity="center"
            android:textColor="@color/text_primary"
            android:textSize="@dimen/text_sm"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
