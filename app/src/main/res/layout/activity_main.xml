<?xml version="1.0" encoding="utf-8"?>
<!--
    Main Activity Layout
    - LinearLayout vertical (course p139-141)
    - FrameLayout for fragment container (course p137-138)
    - BottomNavigationView at bottom (course p557-559)
    "Limited to 5 locations max"
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_main">

    <!--
        Fragment Container (course p137 - FrameLayout)
        Fills all remaining space above BottomNav
    -->
    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

    <!--
        BottomNavigationView (course p557-559)
        - app:menu references bottom_nav_menu.xml
        - Listener set in MainActivity.kt
        - Height >= 48dp for accessibility (course p192)
    -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="@dimen/bottom_nav_height"
        android:background="@color/white"
        app:itemIconTint="@color/nav_color_selector"
        app:itemTextColor="@color/nav_color_selector"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/bottom_nav_menu" />

</LinearLayout>